pipeline {
    agent any
    stages {
        stage('create docker image') {
            steps {
				echo " =========== start building image ========== "
				dir ('tools'){
					sh 'docker build . '
				}
                
            }
        }
    }
}